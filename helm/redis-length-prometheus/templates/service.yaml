apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-length-prometheus.fullname" . }}
  labels:
    {{- include "redis-length-prometheus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redis-length-prometheus.selectorLabels" . | nindent 4 }}
